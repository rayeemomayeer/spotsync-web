# SpotSync web — public env (safe to commit empty values)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081/api/v1
NEXT_PUBLIC_BFF_URL=http://localhost:4000
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_SENTRY_DSN=
# Optional server-only DSN (defaults to NEXT_PUBLIC_SENTRY_DSN)
# SENTRY_DSN=
NEXT_PUBLIC_FEATURE_FLAGS=stripe_billing
